After collecting user answers, analyze the configuration.

Based on user input, decide which ORKA skills to activate:

Rules:

- If language is PHP → use php-backend
- If database is MySQL/MariaDB → use mysql-manager or mariadb-manager
- If HTML/CSS → use html-builder and css-designer
- If Tailwind/Bootstrap → use css-designer
- If React → use react-app
- If Vue → use vue-app
- If Astro → use astro-site
- If Next.js → use nextjs-app
- If Remix → use remix-app

Then:

1. Prepare project folder structure.
2. Generate starter files using selected skills.
3. Follow clean architecture.
